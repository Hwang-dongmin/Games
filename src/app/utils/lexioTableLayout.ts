/** 테이블 중앙 현재 패(CenterPlay3D) 그룹 월드 Z — 플레이어(카메라) 쪽으로 당김.
 *  값을 키우면 제출 패가 가까워지고, 버림패는 뒤·양옆 구역(lexioDiscardLayout)이 넓어짐. */
export const LEXIO_CENTER_PLAY_TABLE_Z = 0.28;
/** 펠트 윗면 반경 (TableRoom 실린더 ~2.15) */
export const LEXIO_TABLE_PLAY_RADIUS = 2.15;
